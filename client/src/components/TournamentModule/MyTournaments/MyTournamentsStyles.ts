import styled from 'styled-components';
import { 
  PUNTIPIN_GREEN,
} from '../../../constants/COLORS';

export const MyTournamentsWrapper = styled.section`
  background: ${PUNTIPIN_GREEN};
`;

export const MyTournamentsContainer = styled.section`
  background: white;
  min-height: 100vh;
`;