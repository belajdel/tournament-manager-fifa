import React from 'react';

/**
 * Styles
 */
import { Container } from '../../../styles/Global';
import {
  LocalChannelContainer,
  LocalChannelWrapper,
} from './LocalChannelStyles';

class LocalChannel extends React.Component {
  render() {
    return (
      <LocalChannelWrapper>
        <Container>
          <LocalChannelContainer>


            
          </LocalChannelContainer>
        </Container>
      </LocalChannelWrapper>
    );
  }
}

export default LocalChannel;