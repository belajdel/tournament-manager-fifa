import React from 'react';

/**
 * Styles
 */
import {
  ChannelSettingsContainer,
} from './ChannelSettingsStyles';

class ChannelSettings extends React.Component {
  render() {
    return (
      <ChannelSettingsContainer>

      </ChannelSettingsContainer>
    );
  }
}

export default ChannelSettings;